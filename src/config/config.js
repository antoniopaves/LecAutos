//Conexión Base de Datos
const config = {
    client: 'mysql',
    connection: {
        host : 'localhost',
        port : 3306,
        user : 'root',
        password : '',
        database: 'db-lecautos'
    }
}

export default config